import { APIResource } from "../../core/resource.mjs";
import { APIPromise } from "../../core/api-promise.mjs";
import { DefaultFlatPagination, type DefaultFlatPaginationParams, PagePromise } from "../../core/pagination.mjs";
import { RequestOptions } from "../../internal/request-options.mjs";
/**
 * External Connections operations
 */
export declare class PhoneNumbers extends APIResource {
    /**
     * Return the details of a phone number associated with the given external
     * connection.
     *
     * @example
     * ```ts
     * const phoneNumber =
     *   await client.externalConnections.phoneNumbers.retrieve(
     *     '1234567889',
     *     { id: '1293384261075731499' },
     *   );
     * ```
     */
    retrieve(phoneNumberID: string, params: PhoneNumberRetrieveParams, options?: RequestOptions): APIPromise<PhoneNumberRetrieveResponse>;
    /**
     * Asynchronously update settings of the phone number associated with the given
     * external connection.
     *
     * @example
     * ```ts
     * const phoneNumber =
     *   await client.externalConnections.phoneNumbers.update(
     *     '1234567889',
     *     { id: '1293384261075731499' },
     *   );
     * ```
     */
    update(phoneNumberID: string, params: PhoneNumberUpdateParams, options?: RequestOptions): APIPromise<PhoneNumberUpdateResponse>;
    /**
     * Returns a list of all active phone numbers associated with the given external
     * connection.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const externalConnectionPhoneNumber of client.externalConnections.phoneNumbers.list(
     *   '1293384261075731499',
     * )) {
     *   // ...
     * }
     * ```
     */
    list(id: string, query?: PhoneNumberListParams | null | undefined, options?: RequestOptions): PagePromise<ExternalConnectionPhoneNumbersDefaultFlatPagination, ExternalConnectionPhoneNumber>;
}
export type ExternalConnectionPhoneNumbersDefaultFlatPagination = DefaultFlatPagination<ExternalConnectionPhoneNumber>;
export interface ExternalConnectionPhoneNumber {
    acquired_capabilities?: Array<'FirstPartyAppAssignment' | 'InboundCalling' | 'Office365' | 'OutboundCalling' | 'UserAssignment'>;
    /**
     * Identifies the civic address assigned to the phone number.
     */
    civic_address_id?: string;
    /**
     * The iso country code that will be displayed to the user when they receive a call
     * from this phone number.
     */
    displayed_country_code?: string;
    /**
     * Identifies the location assigned to the phone number.
     */
    location_id?: string;
    /**
     * Phone number ID from the Telnyx API.
     */
    number_id?: string;
    /**
     * Phone number in E164 format.
     */
    telephone_number?: string;
    /**
     * Uniquely identifies the resource.
     */
    ticket_id?: string;
}
export interface PhoneNumberRetrieveResponse {
    data?: ExternalConnectionPhoneNumber;
}
export interface PhoneNumberUpdateResponse {
    data?: ExternalConnectionPhoneNumber;
}
export interface PhoneNumberRetrieveParams {
    /**
     * Identifies the resource.
     */
    id: string;
}
export interface PhoneNumberUpdateParams {
    /**
     * Path param: Identifies the resource.
     */
    id: string;
    /**
     * Body param: Identifies the location to assign the phone number to.
     */
    location_id?: string;
}
export interface PhoneNumberListParams extends DefaultFlatPaginationParams {
    /**
     * Filter parameter for phone numbers (deepObject style). Supports filtering by
     * phone_number, civic_address_id, and location_id with eq/contains operations.
     */
    filter?: PhoneNumberListParams.Filter;
}
export declare namespace PhoneNumberListParams {
    /**
     * Filter parameter for phone numbers (deepObject style). Supports filtering by
     * phone_number, civic_address_id, and location_id with eq/contains operations.
     */
    interface Filter {
        civic_address_id?: Filter.CivicAddressID;
        location_id?: Filter.LocationID;
        phone_number?: Filter.PhoneNumber;
    }
    namespace Filter {
        interface CivicAddressID {
            /**
             * The civic address ID to filter by
             */
            eq?: string;
        }
        interface LocationID {
            /**
             * The location ID to filter by
             */
            eq?: string;
        }
        interface PhoneNumber {
            /**
             * The phone number to filter by (partial match)
             */
            contains?: string;
            /**
             * The phone number to filter by (exact match)
             */
            eq?: string;
        }
    }
}
export declare namespace PhoneNumbers {
    export { type ExternalConnectionPhoneNumber as ExternalConnectionPhoneNumber, type PhoneNumberRetrieveResponse as PhoneNumberRetrieveResponse, type PhoneNumberUpdateResponse as PhoneNumberUpdateResponse, type ExternalConnectionPhoneNumbersDefaultFlatPagination as ExternalConnectionPhoneNumbersDefaultFlatPagination, type PhoneNumberRetrieveParams as PhoneNumberRetrieveParams, type PhoneNumberUpdateParams as PhoneNumberUpdateParams, type PhoneNumberListParams as PhoneNumberListParams, };
}
//# sourceMappingURL=phone-numbers.d.mts.map