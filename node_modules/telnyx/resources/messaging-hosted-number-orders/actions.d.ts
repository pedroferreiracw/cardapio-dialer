import { APIResource } from "../../core/resource.js";
import * as Shared from "../shared.js";
import { APIPromise } from "../../core/api-promise.js";
import { type Uploadable } from "../../core/uploads.js";
import { RequestOptions } from "../../internal/request-options.js";
/**
 * Manage your messaging hosted numbers
 */
export declare class Actions extends APIResource {
    /**
     * Upload hosted number document
     *
     * @example
     * ```ts
     * const response =
     *   await client.messagingHostedNumberOrders.actions.uploadFile(
     *     'id',
     *   );
     * ```
     */
    uploadFile(id: string, body?: ActionUploadFileParams | null | undefined, options?: RequestOptions): APIPromise<ActionUploadFileResponse>;
}
export interface ActionUploadFileResponse {
    data?: Shared.MessagingHostedNumberOrder;
}
export interface ActionUploadFileParams {
    /**
     * Must be the last month's bill with proof of ownership of all of the numbers in
     * the order in PDF format.
     */
    bill?: Uploadable;
    /**
     * Must be a signed LOA for the numbers in the order in PDF format.
     */
    loa?: Uploadable;
}
export declare namespace Actions {
    export { type ActionUploadFileResponse as ActionUploadFileResponse, type ActionUploadFileParams as ActionUploadFileParams, };
}
//# sourceMappingURL=actions.d.ts.map