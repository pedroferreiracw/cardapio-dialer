// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../../core/resource.mjs";
import * as ActionsAPI from "./actions.mjs";
import { Actions } from "./actions.mjs";
import { DefaultFlatPagination } from "../../core/pagination.mjs";
import { path } from "../../internal/utils/path.mjs";
/**
 * Number reservations
 */
export class NumberReservations extends APIResource {
    constructor() {
        super(...arguments);
        this.actions = new ActionsAPI.Actions(this._client);
    }
    /**
     * Creates a Phone Number Reservation for multiple numbers.
     *
     * @example
     * ```ts
     * const numberReservation =
     *   await client.numberReservations.create();
     * ```
     */
    create(body, options) {
        return this._client.post('/number_reservations', { body, ...options });
    }
    /**
     * Gets a single phone number reservation.
     *
     * @example
     * ```ts
     * const numberReservation =
     *   await client.numberReservations.retrieve(
     *     'number_reservation_id',
     *   );
     * ```
     */
    retrieve(numberReservationID, options) {
        return this._client.get(path `/number_reservations/${numberReservationID}`, options);
    }
    /**
     * Gets a paginated list of phone number reservations.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const numberReservation of client.numberReservations.list()) {
     *   // ...
     * }
     * ```
     */
    list(query = {}, options) {
        return this._client.getAPIList('/number_reservations', (DefaultFlatPagination), {
            query,
            ...options,
        });
    }
}
NumberReservations.Actions = Actions;
//# sourceMappingURL=number-reservations.mjs.map