// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../../core/resource.mjs";
import { DefaultFlatPagination } from "../../core/pagination.mjs";
import { path } from "../../internal/utils/path.mjs";
/**
 * Endpoints related to porting orders management.
 */
export class Comments extends APIResource {
    /**
     * Creates a new comment for a porting order.
     *
     * @example
     * ```ts
     * const comment = await client.portingOrders.comments.create(
     *   '182bd5e5-6e1a-4fe4-a799-aa6d9a6ab26e',
     * );
     * ```
     */
    create(id, body, options) {
        return this._client.post(path `/porting_orders/${id}/comments`, { body, ...options });
    }
    /**
     * Returns a list of all comments of a porting order.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const commentListResponse of client.portingOrders.comments.list(
     *   '182bd5e5-6e1a-4fe4-a799-aa6d9a6ab26e',
     * )) {
     *   // ...
     * }
     * ```
     */
    list(id, query = {}, options) {
        return this._client.getAPIList(path `/porting_orders/${id}/comments`, (DefaultFlatPagination), { query, ...options });
    }
}
//# sourceMappingURL=comments.mjs.map